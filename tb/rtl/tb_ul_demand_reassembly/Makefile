# Standalone Makefile for tb_ul_demand_reassembly.
# Convertible to T0's `tb/rtl/Makefile.inc` by replacing the rule block with
# `include $(REPO_ROOT)/tb/rtl/Makefile.inc` and keeping the variable defs.

REPO_ROOT ?= ../../..
TB_NAME   := tb_ul_demand_reassembly
TB_SRCS   := tb_ul_demand_reassembly.v $(REPO_ROOT)/rtl/umac/tetra_ul_demand_reassembly.v

include $(REPO_ROOT)/tb/rtl/Makefile.inc
