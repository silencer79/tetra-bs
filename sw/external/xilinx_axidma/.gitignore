# Vendoring artefacts — populated by scripts/build/vendor-axidma.sh.
# These live in the working tree but are NOT committed; the canonical
# pin lives in COMMIT_HASH (which IS committed) and the README's
# "Pinned commit:" line.
.upstream/
driver/
library/
examples/
scripts/
LICENSE
*.ko
*.o
*.mod
*.mod.c
*.cmd
modules.order
Module.symvers
